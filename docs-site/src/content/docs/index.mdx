---
title: Retriever
slug: /
description: Build closed-loop robot agents with explicit time.
template: splash
---

<div class="hero-panel">
  <p class="eyebrow">Retriever runtime</p>
  <h1>Build robot agents whose perception, reasoning, and control run together.</h1>
  <p class="lede">Retriever is a Python framework for closed-loop robot systems with explicit timing, typed handoff, graph inspection, and replayable execution.</p>
  <div class="action-grid">
    <a class="action-card" href="/getting-started/visual-quickstart/">
      <strong>Run the visual demo</strong>
      <span>Start with mock color detection, then switch to webcam and Rerun.</span>
    </a>
    <a class="action-card" href="/tutorials/examples-and-results/">
      <strong>Follow the notebook path</strong>
      <span>Commands paired with expected output and what each graph teaches.</span>
    </a>
    <a class="action-card" href="/tutorials/debug-and-visualize/">
      <strong>Debug deliberately</strong>
      <span>Render graphs, step locally, record events, and replay the run.</span>
    </a>
  </div>
</div>

Use the source checkout for demos until the first public package release is live:

```bash
git clone https://github.com/openretriever/retriever.git
cd retriever
pixi install
pixi run demo-webcam-detection-mock
```

After `retriever-core==0.0.1` resolves from PyPI, the library install path becomes:

```bash
python -m pip install retriever-core
```

General-purpose robot agents rarely fit in one clean loop. Cameras stream, robot state updates faster than policies, VLM/VLA calls have variable latency, planners can block, operators intervene, and replay has to explain exactly what happened. Retriever puts timing and handoff decisions into the graph instead of scattering them across callbacks, queues, and sleeps.

<figure class="figure-card">
  <img src="/assets/figures/01_pipeline.png" alt="Closed-loop Retriever robot agent pipeline." />
  <figcaption>A representative Retriever pipeline: slow reasoning, medium-rate skills, and high-rate control live in one explicit graph.</figcaption>
</figure>

## Choose Your Path

<div class="card-grid">
  <a class="info-card" href="/getting-started/visual-quickstart/"><strong>Fastest first success</strong><span>Run mock color detection. It needs no camera, GUI, or robot backend.</span></a>
  <a class="info-card" href="/concepts/flow/"><strong>Understand the programming model</strong><span>Learn the Flow class, typed IO, and why `step(...)` stays ordinary Python.</span></a>
  <a class="info-card" href="/concepts/time-and-sync/"><strong>Understand explicit time</strong><span>Learn clocks, event streams, and sync policies before debugging backend timing.</span></a>
  <a class="info-card" href="/ecosystem/golden-packs/"><strong>Use Golden reference modules</strong><span>GoldenRetriever is the applied layer: robot type packs, examples, simulator wrappers, and Hub-pack candidates.</span></a>
</div>


## After The Core Quickstart

Retriever core teaches the runtime contract. GoldenRetriever is the applied continuation: run the Hub-pack proof, then move through maintained robot-facing examples for perception, memory, language, composition, simulation, and visualization.

<div class="card-grid">
  <a class="info-card" href="/ecosystem/golden-packs/"><strong>Prove the Hub boundary</strong><span>Load Golden robot payloads through Retriever Hub from a local source checkout.</span></a>
  <a class="info-card" href="https://retriever-space.pages.dev/examples/"><strong>Browse applied examples</strong><span>Use Golden as the maintained reference catalog after the core visual demo works.</span></a>
  <a class="info-card" href="https://github.com/openretriever/golden-retriever"><strong>Open Golden source</strong><span>Inspect the runnable examples, notebooks, type packs, and public-surface checks.</span></a>
</div>

## The Four Objects

<div class="card-grid">
  <a class="info-card" href="/concepts/flow/"><strong>Flow</strong><span>A typed Python class with local state and a synchronous `step(...)` method.</span></a>
  <a class="info-card" href="/concepts/time-and-sync/"><strong>Clock</strong><span>Each Flow declares when it runs; there is no global robot timestep.</span></a>
  <a class="info-card" href="/concepts/time-and-sync/"><strong>Sync policy</strong><span>Each edge declares how upstream event history becomes one aligned input.</span></a>
  <a class="info-card" href="/concepts/runtime/"><strong>Pipeline</strong><span>The graph that validates, visualizes, steps, replays, and runs.</span></a>
</div>

## For AI Agents

Agents should start from `llms.txt`, then run the mock visual smoke before changing code. The reliable first command is:

```bash
pixi run demo-webcam-detection-mock
```

Use `/tutorials/examples-and-results/` for expected outputs and `/tutorials/debug-and-visualize/` for graph/render/replay commands.
