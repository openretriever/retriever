---
title: "Visual Quickstart: Color Detection"
description: Run the first visual Retriever demo.
---
Start here if you want to see Retriever do something immediately. The first run is deterministic and hardware-free; the second run uses a real webcam and Rerun when available.

Run one copy-paste path first:

```bash
git clone https://github.com/openretriever/retriever.git
cd retriever
pixi install
pixi run demo-webcam-detection-mock
```

This is a source-checkout demo target. The package install target is `retriever-core`, but examples, Rerun, graph rendering, and tutorial assets live in the repository environment.

<div class="notebook-takeaway"><strong>Success criterion:</strong> the mock command runs without a camera, GUI, Rerun viewer, or robot backend. If you only want to read first, jump to the typical output and compare it later.</div>

Typical output:

```text
Perception Demo - Live or Mock Camera to Detection

Building perception pipeline:
  Camera @ Rate(20Hz) -> ColorDetector @ Trigger -> Display @ Rate

[ok] Graph created: 3 nodes, 5 edges

Running for 0.1 seconds...
Tip: This run is using mock frames. Use --camera-mode real to require a live webcam path.
------------------------------------------------------------
  Frame 1: 2 objects - [('red_object', '0.95'), ('blue_object', '0.95')]
```

What happened:

- `CameraSource` emitted synthetic image frames,
- `ColorDetector` sampled the latest frame when its trigger fired,
- `DisplayFlow` printed detections,
- the graph ran without camera permission, GUI windows, Rerun, or backend setup.

## Step 2: Switch to Webcam and Rerun

```bash
pixi run demo-webcam-detection
```

This uses the same tutorial pipeline with real camera input. On a machine with a working viewer, Rerun shows image frames and detections over time. If Rerun is unavailable, the command still emits stdout events so you can separate runtime correctness from viewer setup.

## Step 3: Pick the Right Variant

| Need | Command |
| --- | --- |
| Reliable first smoke, no hardware | `pixi run demo-webcam-detection-mock` |
| Live webcam with automatic Rerun/stdout fallback | `pixi run demo-webcam-detection` |
| Live webcam with multiprocessing and forced Rerun | `pixi run demo-webcam-detection-mp-rerun` |
| Mock frames with forced Rerun | `pixi run python -m examples.tutorial.b_ir_and_execution.06_dora_perception --backend in-process --camera-mode mock --visualize rerun --duration 10` |
| Portable recording for replay/debugging | `pixi run demo-webcam-record` |

## Step 4: If It Looks Wrong

| Symptom | Try first | Why |
| --- | --- | --- |
| No camera or camera permission issue | `pixi run demo-webcam-detection-mock` | Removes hardware from the question. |
| Rerun viewer does not open | Use `--visualize stdout` | Keeps runtime verification separate from viewer setup. |
| Graph wiring is unclear | `pixi run docs-tutorial-perception-html` | Shows Flow nodes, ports, clocks, and sync policies. |
| Detection output is surprising | `pixi run demo-perception-stepper` | Steps the detector with deterministic mock frames. |
| Behavior changes between runs | `pixi run demo-webcam-record` then replay | Saves the input stream as a reusable artifact. |

## Why This Is First

The color-detection path is small but representative: it has sensor input, typed Flow outputs, asynchronous timing, visualization, and an immediate debugging story. The mock run proves the graph without hardware; the webcam run proves the live visual path. After this works, the same graph can be stepped, inspected, recorded, replayed, and moved to another backend.

<div class="card-grid">
  <a class="info-card" href="/tutorials/examples-and-results/"><strong>See more example outputs</strong><span>Notebook-style cells for Flow, stepping, perception, graph render, and replay.</span></a>
  <a class="info-card" href="/tutorials/debug-and-visualize/"><strong>Debug this graph</strong><span>Render the graph, step it locally, record events, and replay them.</span></a>
  <a class="info-card" href="/concepts/time-and-sync/"><strong>Understand the timing</strong><span>Learn why Retriever uses clocks and per-edge sync policies instead of one global loop.</span></a>
  <a class="info-card" href="https://retriever-space.pages.dev/examples/"><strong>Continue to Golden examples</strong><span>Move from the core visual demo to maintained robot-facing examples, robosuite mock traces, and HTML graph visualization.</span></a>
</div>
